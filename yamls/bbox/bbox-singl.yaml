apiVersion: v1
kind: Pod
metadata:
  name: bbox-single
  namespace: default
spec:
  containers:
  - name: busybox
    image: busybox
    command:
    - sleep
    - "3600"
    volumeMounts:
    - mountPath: /data
      name: xx123
  volumes:
  - name: xx123
    persistentVolumeClaim:
      claimName: bbox-data
  restartPolicy: Always
